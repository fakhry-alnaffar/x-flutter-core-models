name: onix_flutter_core_models
description: Onix Flutter core models — failure types, server error hierarchy, and progress state for domain layers.
version: 0.1.0
repository: https://github.com/fakhry-alnaffar/onix-flutter-core-models

topics:
  - failure
  - models
  - domain
  - networking

environment:
  sdk: ^3.0.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
